const Employee = require("../lib/Employee");

describe("Employee", () => {
    describe("Initialization", ()=> {
        it("should return an error if anything other than 3 arguments are called with the constructor", ()=>{
            //Create a callback function for 0,1,2,5 arguements
            //mock the console.log
            // check against error message
            const nullEmployee = new Employee();
        });

        it("should return an object with a 'string' property when called with the new keywords")

    });

});